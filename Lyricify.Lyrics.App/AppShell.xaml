<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Lyricify.Lyrics.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Lyricify.Lyrics.App.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.TabBarBackgroundColor="#1A1A1A"
    Shell.TabBarForegroundColor="{StaticResource ActiveLineColor}"
    Shell.TabBarUnselectedColor="{StaticResource InactiveLineColor}"
    Shell.TabBarTitleColor="{StaticResource ActiveLineColor}"
    BackgroundColor="{StaticResource PageBackground}">

    <TabBar>
        <ShellContent
            Title="Lyrics"
            ContentTemplate="{DataTemplate views:LyricsPage}"
            Icon="music_note.png" />
        <ShellContent
            Title="Settings"
            ContentTemplate="{DataTemplate views:SettingsPage}"
            Icon="settings.png" />
    </TabBar>
</Shell>
